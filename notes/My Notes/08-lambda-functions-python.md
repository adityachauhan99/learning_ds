
lambda num : num+2
anonymous functions or tiny functions and they have no name and only have one expression as their body

(num) : argument
(num+2) : expression

the expression has to return a value, a statement doesn't

addition = lambda a,b : a+b
print(addition(2,4))


*When a function is a one liner, it's commonly used by using lambda function*
