
key:value pair

eg: dog = {"name" : "roger" , "age" : 8, "color" : "brown"}


dictionary_name.get("key name")= value
dictionary_name.get("color","green") = if the color is a valid key name, it will give the value assigned to it otherwise it will give green as output

dictionary_keys() = gives all the keys present
dictionary_values() = gives all the values present
