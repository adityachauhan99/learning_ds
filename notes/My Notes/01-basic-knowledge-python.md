input function : input("Your String")
	type: String (by default)

converting into a specific type:
	integer = int()
	 float = float()
	 etc.

typecasting an input into a specific type:
	 integer : int(input("Enter any number:"))        ----------------  this will make sure the number is always an integer

