
def add(a,b,c):
	 print(a+b+c)
nums=[1,2,3]
add("star"nums)

this breaks the list into 1,2,3 and uses them as arguments.

another example:
print("star"range(1,6),sep="")

breaks the range into 1,2,3,4,5

name, "star"line=["Tina",70.5,80.5]

after this : name has TIna 
         line has [70.5,80.5]