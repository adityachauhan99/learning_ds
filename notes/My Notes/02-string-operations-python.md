Uppercase : str_name.upper()
Lowercase: str_name.lower()

==STRINGS ARE IMMUTABLE ------------------- can't change the string
WHENEVER WE ADD A CHANGE, A NEW STRING IS CREATED== 

str_name.find("sub_string") : gives the index of that sub string as output

replace : string.replace("Value which you want to replace", "The new replaced value")

check for presence : "sub string" ==in== name_of_the_string

a="This is a string"
a=a.split(" ")    # splits the string after each space , makes a list of strings 
a="-".join(a)    # adds the strings back with "-" between them


isalnum() - alpha numeric
isalpha() - alphabet
isdigit() - numbers 
islower() - lowercase
isupper() - uppercase 



